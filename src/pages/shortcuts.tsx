import { Component } from 'solid-js';
import RenderMarkdown from '../global/markdown';

const Shortcuts: Component = () => {

    return <RenderMarkdown file='shortcuts.md' />;
};

export default Shortcuts;
