import { Component } from 'solid-js';
import RenderMarkdown from '../global/markdown';

const Install: Component = () => {

    return <RenderMarkdown file='install.md' />;
};

export default Install;
